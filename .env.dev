# Development Environment Configuration
NODE_ENV=development
PORT=8080

# Database Configuration (Development MySQL)
DATABASE_URL=mysql://llmmux_dev:dev_password@localhost:3307/llmmux_dev

# JWT Authentication (Development)
JWT_SECRET=development-jwt-secret-change-in-production

# Inference Servers (Development)
INFERENCE_SERVERS=localhost:8000,localhost:8001

# CORS Configuration (Permissive for development)
ENABLE_CORS=true
CORS_ORIGIN=*

# Logging (Verbose for development)
LOG_LEVEL=debug

# Health Check Configuration
HEALTH_CHECK_INTERVAL=30000

# Development Database Configuration (for Docker Compose)
MYSQL_ROOT_PASSWORD=dev_root_password
MYSQL_DATABASE=llmmux_dev
MYSQL_USER=llmmux_dev
MYSQL_PASSWORD=dev_password
